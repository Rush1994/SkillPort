# 安全说明

## 报告问题

项目目前处于开发预览阶段，尚无稳定版本支持周期或承诺的响应时限。

如果公开仓库启用了私密漏洞报告，请优先使用该入口。若尚未启用，请先提交不含漏洞细节的问题，请维护者提供私密联系方式；不要在公开讨论中发布凭据、可利用细节、内部地址或受影响的私有 Skill。当前不指定尚未设立的邮箱。

私密报告应包含受影响版本或提交、平台、复现步骤、最小无秘密样本和预期影响。普通使用问题见[贡献指南](CONTRIBUTING.md)。

## 连接默认值

自动模式会无凭据探测 HTTPS，再按需探测 HTTP。自动 HTTPS 和无匹配配置的直接 HTTPS 引用默认接受不受信任证书；HTTP 为明文。需要服务器身份校验时，使用 `--insecure=false` 或配置企业 CA。详细优先级见[使用指南](docs/usage.md)。

探测不携带 Registry 凭据，不跟随重定向；选定协议后再读取认证信息。认证后的错误不触发协议切换，跨主机 token realm 和重定向当前拒绝。

## 文件与凭据边界

- Skill 内容及元数据不作为命令执行。
- Docker 配置和凭据助手只读使用，不触发注销或保存登录。
- 下载先校验内容哈希和路径，再写入目标；链接、越界和不兼容路径被拒绝。
- 本地 `.skillport` 保存基线文件副本，备份保存旧目录；两者也可能包含用户此前同步的内容，应按本地数据管理。
- 敏感扫描仅覆盖有限规则。二进制、压缩内容或超限文本可能未扫描，可用 `--strict` 拒绝这些警告。
- 标签复查不是并发事务。严格不覆盖需唯一标签和 Harbor 不可变策略。

完整的强杀/断电恢复与跨平台故障验收仍在[开发路线](docs/roadmap.md)中，不能把开发预览视为已完成安全审计。
